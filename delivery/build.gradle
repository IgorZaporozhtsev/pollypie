plugins {
    id 'java'
    id 'com.google.cloud.tools.jib'
}

jib.to.image = "booratina/${project.name}:${version}"

dependencies {
    implementation project(path: ':kafka')
    implementation project(path: ':common')
}
