plugins {
    id 'java'
    id 'com.google.cloud.tools.jib'
}

jib.to.image = "booratina/${project.name}:${version}"

dependencies {
    compileOnly project(path: ':kafka')
    compileOnly project(path: ':common')
}

