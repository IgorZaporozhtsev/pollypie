plugins {
    id 'java'
    id 'com.google.cloud.tools.jib'
}

jib.to.image = "booratina/${project.name}:${version}"

dependencies {
    implementation project(path: ':kafka') //Could not up Application context for test if compileOnly
    implementation project(path: ':common')
}

